# گزارش مقایسهٔ کامل پوشه‌های `my` و `test`

تاریخ: ۲۰۲۶-۰۹-۱۹ · تهیه‌شده توسط ایجنت — بدون حدس؛ همه‌چیز از روی فایل‌ها و کامنت‌های داخل کد استخراج شده.

---

## ۱) هویت دو نسخه (این مهم‌ترین یافته است)

| ویژگی | پوشهٔ `my` | پوشهٔ `test` |
|---|---|---|
| تاریخ فایل‌ها | تا ۱۲ سپتامبر | تا ۱۹ سپتامبر (امروز) |
| نقش | دموی مستقل «هم‌مسیر» (به گفتهٔ خودش: «دموی مستقل هم‌مسیر — تنظیمات آماده؛ فایل‌مود؛ بدون دیتابیس؛ حساب‌های دمو» — `my/config/config.php`) | کپی تست کامل اپ (`test/config/config.php`: «کپی تست — کاملاً جدا از سایت اصلی؛ فایل‌مود») |
| فرانت‌اند | طرح قدیمی v1 — فقط `assets/css/joma.css` | طرح نسخهٔ ۲۰/۲۱ — `joma-v2.css` + `joma-companion.css` + پوستهٔ `v2_shell.php` |
| RewriteBase | `/demo/` | `/test/` |
| کد امنیتی ثبت‌نام | **خاموش** (`registration_security_code_enabled => false`) | **روشن** (کد `J0m@14O5`) |
| کاربران فایل داده | ۶ حساب دمو (demo, drahmadi, sara, ali, maryam, hossein) | ۳ حساب (modir, maryam, moshaver) |
| خط پایان فایل‌ها | CRLF | LF |

**نتیجه:** `test` یک «ابر مجموعه» از همان پایهٔ مشترک است که حدود یک هفته بیشتر تکامل یافته. `my` عملاً عکس قدیمی‌تر + چند آیتم خاص خودش است.

---

## ۲) چیزهایی که در `my` هست و در `test` نیست

فقط **یک فایل** به‌طور کامل منحصربه‌فرد `my` است: `info.php` (پراب «PHP-OK» برای بررسی سلامت هاست).
بقیه تفاوت‌ها **داخل فایل‌های مشترک** هستند:

| # | قابلیت (فقط در my) | جای دقیق | ماهیت واقعی |
|---|---|---|---|
| M1 | صفحهٔ «درباره جوما» به‌صورت **صفحهٔ عمومی مستقل با بستهٔ کامل سئو** (43 کیلوبایت؛ متا تگ‌های Open Graph، canonical، robots، توضیحات فارسی غنی) | `my/pages/about.php` | در `test` همان محتوا به داخل پوستهٔ اپ منتقل شده (کامنت خودش: «محتوای صفحهٔ قبلی حفظ شده») اما **لایهٔ سئوی عمومی** (og:/canonical/robots و ساختار مستقل) از بین رفته |
| M2 | دو کارت ویژه در صفحهٔ خانه: «تمرینات تخصصی طرحواره درمانی (۲۷ تمرین)» و «تمرینات تخصصی زوج درمانی (۳۵ تمرین)» با لینک به `learn&g=schema / couple` | `my/pages/home.php` (کلاس‌های `home-spec-*`) | **خود تمرین‌ها در هر دو هست** (`content/learn_guides.json` در هر دو کاملاً یکسان، ۶۲ راهنما)؛ فقط ویترین/نقطهٔ ورودش در خانهٔ `test` نیست |
| M3 | نمایش متن «یادداشت (note)» مراجع به مشاور + ارسال فیلد `note` در پاسخ‌های هم‌مسیر | `my/pages/hammasir_provider.php` و `my/functions/hammasir.php` | در `test` **عمداً حذف شده** — کامنت صریح: «(تصمیم مالک — B4): متن یادداشت به مشاور نمی‌رود؛ تا مجوز جدا (VIEW_NOTES) ساخته نشود» |
| M4 | گیت ثبت حال روی **بیشتر صفحه‌ها** (لیست استثنا: mood, logout, about, login, register, forgot, home, learn) | `my/includes/helpers.php` | در `test` **عمداً محدود شده** به فقط صفحهٔ «امروز» — کامنت صریح: «(تصمیم مالک — B2): گیت ثبت حال فقط روی صفحهٔ امروز می‌ماند» |
| M5 | بازیابی رمز **مستقیم** (شناسه + رمز جدید، بدون کد یک‌بارمصرف) | `my/pages/forgot.php` | در `test` با جریان امن‌تر OTP جایگزین شده (B1: کد یک‌بارمصرف ۱۵ دقیقه، ۵ تلاش + صفحهٔ مدیر صدور کد) |
| M6 | جعبهٔ سوییچ حالت هم‌مسیر داخل **سایدبار** (فرم‌های تغییر حالت کنار منو) | `my/includes/layout.php` | در `test` هم سوییچ حالت هست ولی **داخل خود صفحهٔ هم‌مسیر** (POST + CSRF به `mode_set`) نه سایدبار |
| M7 | ثبت‌نام بدون کد امنیتی + حساب‌های دمو | `my/config/config.php`, `my/data/store.json` | تنظیمات محیط دمو — نه یک قابلیت محصولی |
| M8 | `info.php` | ریشهٔ `my` | ابزار بررسی سلامت هاست |

---

## ۳) قابلیت‌های مشترک (در هر دو موجود)

**هستهٔ جوما:** ورود / ثبت‌نام / خروج / فراموشی رمز · دوره‌های شمسی مستقل (`periods`, `period`) · ساخت و نهایی‌سازی برنامه (`plan`) · ثبت عملکرد روزانه (`today`) · ثبت حال با ۵ شاخص (انرژی، حال عمومی، تمرکز، خواب، استرس) · گزارش‌ها با نمودار (`reports`) · کتابخانهٔ ۱۰۷ فعالیت رسمی + جست‌وجو/فیلتر/فعالیت شخصی (`library`) · آموزش ۶۲ راهنما در ۳ گروه جوما/زوج‌درمانی/طرحواره (`learn`) · تنظیمات (حساب + نمایش فشرده + ترجیح اعلان) · پروفایل · پشتیبانی (بله/روبیکا/پیامک — لینک‌ها در هر دو یکسان) · درباره جوما (محتوا در هر دو هست، قالب فرق دارد).

**ماژول هم‌مسیر (در هر دو فعال):** حالت‌های مراجع/مشاور/مدیر + سوییچ حالت امن · انتخاب همراه با رضایت و مجوزها (VIEW_PROGRESS / VIEW_MOOD) · گفتگوی دوطرفه با نشانگر نخوانده · داشبورد مشاور (KPI، آیتم‌های نیازمند توجه، نوار خلق کلینیک، پوشش عملکرد) · دعوت یک‌بارهٔ Onboarding برای کاربر بدون لینک · مدیریت مشاوران و مدیران هم‌مسیر.

**زیرساخت مشترک:** ذخیره‌سازی فایل‌مبنای `data/store.json` · تقویم جلالی · CSRF · گیت ثبت‌نام · آنالیتیکس · توابع موفقیت (success) · `api/username.php`.
(فایل‌های `assets/css/joma.css`، `assets/js/joma.js`، `config/hammasir_config.php`، `content/learn_guides.json`، `database/library_official.json` و همهٔ صفحه‌های `hammasir_admin*` بایت‌به‌بایت یکسان‌اند.)

---

## ۴) چیزهایی که فقط در `test` هست (برای کامل بودن تصویر)

جوجهٔ من (B5) · دفترچهٔ جوما/بینش (B7) · آب با حالت پیش‌نویس/قطعی + لیوان (B6) · کپی برنامه به ماه بعد (B8) · بازیابی رمز با کد یک‌بارمصرف + صفحهٔ مدیر بازیابی (B1-2) · مسدودسازی ثبت عملکرد برای روزهای آینده (B3) · باطل‌شدن نشست‌ها پس از تغییر رمز (`auth_epoch`) · حقوق داده (خروجی CSV/JSON/خوانا + درخواست حذف با مهلت) · کنسول مدیر · بررسی سلامت نصب `health.php` · نظام نقش‌ها و دعوت‌نامهٔ مشاور (C1–C5) · تمرین تنفس ۴-۷-۸ با صدا (`assets/audio/br-*.mp3`) · تقویم «مسیر این ماه» · گزارش‌های ۱۰ تبی جدید (`reports1`, `analytics_reports*`) · پوستهٔ کامل v2 (`v2_shell/sprite/calendar/chick/glass/mood/rings/roles/time`) · مستندات داخلی (فایل‌های ۰۰ تا ۱۲)، تست‌ها و `database/migrations/hammasir_phase1.sql`.

---

## ۵) اگر بخواهیم قابلیت‌های بخش ۲ را به `test` اضافه کنیم — طرح و ریسک (مبنای شما: تا حد امکان ADD-ONLY)

> ⚠️ جهت ادغام هنوز توسط شما تأیید نشده — فرض من در این جدول: **پایه = `test`، افزودنی‌ها از `my`**. (سؤال ۱ را ببینید.)

| آیتم | روش پیشنهادی (ADD-ONLY) | فایل‌هایی که لمس می‌شوند | ریسک |
|---|---|---|---|
| M1 صفحهٔ سئو | افزودن یک صفحهٔ عمومی جدید (مثلاً فایل تازه `pages/about_seo.php` + ثبت کلید جدید در آرایهٔ صفحه‌های عمومی `index.php`) — بدون دست‌زدن به `about` فعلی | ۱ فایل جدید + یک افزودن کوچک به آرایهٔ `index.php` | 🟢 کم |
| M2 کارت‌های خانه | افزودن یک `<section>` جدید به `pages/home.php` با کلاس‌های توکن‌دارِ طرح v2 (نه CSS مستقل از my) | فقط `pages/home.php` | 🟢 کم |
| M3 یادداشت برای مشاور | **برگشت تصمیم مالک B4** — نیازمند تغییر `functions/hammasir.php` + `pages/hammasir_provider.php` (هر دو فایل مشترک و حساس) | ۲ فایل مشترک | 🔴 زیاد (تضاد با تصمیم ثبت‌شده + دادهٔ حساس مراجع) — فقط با حکم صریح شما |
| M4 گیت حال روی همهٔ صفحه‌ها | **برگشت تصمیم مالک B2** — تغییر `includes/helpers.php` | ۱ فایل مشترک | 🟠 متوسط (رفتار فراگیر؛ تجربهٔ کاربری همهٔ صفحه‌ها عوض می‌شود) |
| M5 بازنشانی مستقیم رمز | با OTP در `forgot.php` **تعارض** دارد (یک صفحه، دو جریان متضاد)؛ بازگرداندنش یعنی حذف لایهٔ امنیتی فعلی | ۱ فایل مشترک + حذف منطق امنیتی | 🔴 زیاد — توصیه: منتقل نشود |
| M6 سوییچ حالت در سایدبار | تغییر پوستهٔ `v2_shell`/`layout.php` — برخلاف قانون «فرانت از `test` تبعیت کند» | پوستهٔ مشترک | 🟠 متوسط (ظاهر همهٔ صفحه‌ها لمس می‌شود) |
| M7 کد امنیتی/دمو | فقط تنظیمات محیط تست است؛ به محصول ربطی ندارد | — | — (منتقل نشود) |
| M8 `info.php` | کپی فایل (نسخهٔ فعلی‌اش فقط `PHP-OK` چاپ می‌کند، نه `phpinfo()`) | ۱ فایل جدید | 🟢 کم — ولی روی دامنهٔ اصلی توصیه نمی‌شود |

**جمع‌بندی ریسک:** از ۸ آیتم، ۳ تای آن‌ها (M3، M5، M6) با تصمیم‌های ثبت‌شدهٔ خودتان در خط `test` (B4، B2، B1) یا با قانون فرانت‌اند در تضادند و بدون حکم صریح شما هیچ‌کدام را اجرا نمی‌کنم. ۲ تای آن‌ها (M1، M2) کاملاً ADD-ONLY و کم‌ریسک‌اند. بقیه یا محیطی‌اند یا توصیه به عدم انتقال.

---

## ۶) نکات فنی مهم برای اجرا

1. **قانون فرانت:** همهٔ افزودنی‌ها باید با توکن‌های `joma-v2.css` / `joma-companion.css` و پوستهٔ `v2_shell.php` ساخته شوند. هیچ استایل مستقلی از `my` (که مال طرح v1 است) کپی نخواهد شد.
2. **دیتابیس:** هر دو کپی «فایل‌مبنا» هستند (`storage = file`). فایل `mirbolouki_joma (5).sql` که فرستادید دامپ دیتابیس **MySQL سایت اصلی** است (جدول‌های `joma_users`, `joma_plans`, … + `joma_otp_codes`). سند داخلی خود بسته (`03-نقشه-سایت-اصلی.txt`) هم همین سؤال را دارد که سایت اصلی فایل است یا MySQL — برای انتقال نهایی به دامنهٔ اصلی، این تعیین‌کننده است.
3. **هرگز در ادغام لمس نمی‌شوند:** پوشهٔ `data/` (دادهٔ کاربران)، `config/config.php`، فایل‌های قفل‌شدهٔ فهرست `LOCK-LIST-v8.txt`.
4. فایل‌های `my` با خط پایان CRLF هستند؛ برای ادغام باید نرمال‌سازی شوند (در تحلیل انجام شد).

---

## ۷) سؤالاتی که باید شما جواب بدهید (بدون این‌ها اجرا نمی‌کنم)

1. **جهت ادغام:** پایه `test` باشد و قابلیت‌های `my` رویش اضافه شود؟ (برعکسش با قانون «فرانت از تست تبعیت کند» نمی‌خواند)
2. **M3 (یادداشت مراجع به مشاور):** در `test` به «تصمیم مالک» حذف شده؛ واقعاً برگردد؟
3. **M4 (گیت حال):** فقط روی «امروز» بماند (وضعیت تست) یا به بیشتر صفحه‌ها برگردد (وضعیت my)؟
4. **M5 (بازیابی رمز):** همین OTP بماند یا روش مستقیم قدیمی را می‌خواهید؟
5. **M1 (صفحهٔ سئو):** به‌صورت صفحهٔ عمومی مستقل (با ساختار سئوی خودش) اضافه شود یا با قالب طرح تست بازنویسی شود؟
6. **هدف نهایی:** این ادغام برای ساب‌دامین تست است یا مقدمات انتقال به دامنهٔ اصلی (که دیتابیس MySQL دارد)؟
